{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "StructureRemover",
  "description": "Remove structures from your world",
  "authors": [
    "Nerjal Nosk"
  ],
  "contact": {
    "repo": "https://github.com/NerjalNosk/StructureRemover"
  },
  "icon": "assets/${mod_id}/icon.png",
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
  },
  "mixins": [
    "structure-remover.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": ">=${minecraft_version}"
  }
}
